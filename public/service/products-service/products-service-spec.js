describe('productsService', function() {

  beforeEach(module('public'));

  it('should ...', inject(function(productsService) {

    //expect(productsService.doSomething()).toEqual('something');

  }));

});
